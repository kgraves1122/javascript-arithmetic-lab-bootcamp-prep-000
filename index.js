1 + 80
60 - 40
2 * 3.4
5.0 / 2.5

function add (x,y) {
  var add = x + y;
  return add;
}

function subtract (x, y) {
  var subtract = x - y;
  return subtract;
}

function multiply (x,y) {
  var multiply = x * y;
  return multiply;
}

function divide (x,y) {
  var divide = x / y;
  return divide;
}