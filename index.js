function add (a,b) {
  var add = a + b;
  return add;
}


function subtract (a,b) {
  var subtract = a - b;
  return subtract;
}

function multiply (a,b) { var multiply = a * b;
return multiply;
}

function divide (a,b) {
  var divide = a/b;
  return divide;
}

function inc(n) {
  var num = parseInt(n);
  return num;
}

function dec(n) {
  var too = parseFloat(n);
  return too;
}