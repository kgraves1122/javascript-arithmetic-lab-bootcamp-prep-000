function add (a,b) {
  var add = a + b;
  return add;
}


function subtract (a,b) {
  var subtract = a - b;
  return subtract;
}

function multiply (a,b) { var multiply = a * b;
return multiply;
}

function divide (a,b) {
  var divide = a/b;
  return divide;
}

function inc(n) { 
  var number += n;
  return number;
}

function dec(n) { 
 var num -= n;
 return num;
}

function makeInt(n) {
  if (makeInt(n) !== 'NaN') {
    var number = parseInt(n, 10);
    return number;
  }
  else return 'NaN';
  }

